Captured 2026-09-19 from `opencode acp` 1.18.30 on `opencode/muse-spark-1.3-contributor-free`:
the lane's full prompt (template at gh-workflows `1b1c597`, hash `e775e965…`) over a checkout of
prismalens/sreforge#183 at `49d44133` with `.claude-review-manifest.json` and
`.claude-review.diff` staged. Completed in 92 seconds: manifest and diff read, four review
subagents, no findings, the summary in the agent's text because its `gh pr comment ... | head`
was refused. The checkout path is replaced by `/checkout`. `test/replay.test.js` replays it.
